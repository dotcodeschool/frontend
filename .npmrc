# Supply-chain defense: block freshly published package versions for 7 days.
# Most malicious npm packages are reported and pulled within hours of release,
# so a 7-day window catches the long tail without blocking legitimate updates.
# Applies to pnpm (>=10). Do not lower or remove without team discussion.
minimum-release-age=10080
